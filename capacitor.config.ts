import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bezaleel.controlescolar',
  appName: 'CDE Bezaleel',
  webDir: 'dist'
};

export default config;
